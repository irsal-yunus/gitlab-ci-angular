create install and setup gitlab-runner executor ssh :
- note untuk custom_build_dir only executor kubernetes dan docker
- gitlab-runner register --non-interactive  \
  --custom_build_dir-enabled=true \
  --name=test \
  --url="https://gitlab.com/" \
  --registration-token="xxx" \
  --executor=ssh \
  --cache-path gitlab-ci-cache \
  --cache-shared true
  
  # error : Cleaning up file based variables 00:00 ERROR: Job failed: Process exited with status 1
  fix : - echo 'unset cd'>>~/.bash_profile; source ~/.bash_profile
  


